-- This is an example local configuration file.
-- To use it, copy it to 'lua/config/local.lua' and uncomment the settings you want to change.
-- 'lua/config/local.lua' is ignored by git.

return {
  -- To disable lazy.nvim's auto update checker, uncomment the following line
  -- disable_lazy_update_checker = true,
}
